Problemas encontrados y Solucion del problema
Uno de los principales problemas fue hacer el parsing del archivo input para poder leerlo y meter las reglas de produccion
tuvimos demasiados problemas con este ya que no conociamos muy bien la lectura de lachivo de c++
Luego tuvimos que reformular la forma en la que procesabamos las parejas del archivo ya que lo teniamos de la siguiente manera en una parejas
<no terminal, {producciones}> lo que a nivel del algorimo nos complicaba bastante por lo que decidimos generar las parejas de la esta manera
<no terminal, produccion1>, <no terminal, produccion2>


-Debugging complicado porque el cmake

-Cuando el conjunto de first presenta mas de un elemento y hay mas de 1 posible prodccion para el no terminal ejemplo S -> A | D     el first de S es {a,d}, se complica ya que hay asignar una diferente para cada una
lo mismo cuando en el firstset tenemos epsilon




no hay recursividad S no puede producir -> Sa

no puede haber first con 2 terminales iguales por ejemplo S -> aA aB  no se pu



dificultad para la implementacion del metodo goto
dificultad para la insercion e impresion de la tabla slr
dificultad para el manejo de los follows en los slr

dificultad para referenciar la ruta relativa del archivo example.txt




dificultad procesando epsilon